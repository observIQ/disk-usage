FROM golang:1.13

ARG version

ADD . /disk-usage/
WORKDIR /disk-usage

RUN go test ./...

RUN go build

RUN ./disk-usage
RUN touch /tmp/suppress
RUN ./disk-usage
RUN if [ ! -f /tmp/suppress ]; then echo "lock was removed!"; fi
